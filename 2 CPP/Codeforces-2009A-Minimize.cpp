//tailwind code, performs super fast, as this is a basic arithmetic problem
#include <bits/stdc++.h>
using namespace std;

int main(){
    int nc;
    cin >> nc;
    for(int i=0; i<nc; i++){
        int a,b;
        cin >> a >> b;
        cout << b-a << endl;
    };
}
